return {
  "neovim/nvim-lspconfig",
  opts = {
    servers = {
      -- vhdl_ls = {},
      -- theme_check = {
      --   cmd = { "shopify", "theme", "language-server" },
      --   root_dir = function(fname)
      --     return vim.loop.cwd()
      --   end,
      -- },
      -- markdown_oxide = {},
    },
  },
}
